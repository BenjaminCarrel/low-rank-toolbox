# This workflow has been deprecated in favor of tests.yml
# Please use tests.yml which provides comprehensive testing across multiple platforms

name: Test (Deprecated)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
    - name: Deprecation Notice
      run: |
        echo "⚠️ This workflow is deprecated."
        echo "Please use tests.yml instead for comprehensive testing."
        exit 1
 